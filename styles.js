import { StyleSheet,View, Text } from "react-native";

const styles = StyleSheet.create({
    labelText: {
      marginBottom:5,
      marginLeft:10,
      fontSize:16,
      fontWeight:'bold',
    },
    input: {
      height:40,
      marginBottom:5,
      marginLeft:10,
      borderWidth:1,
      borderRadius:5,
      fontSize:15,
      paddingLeft:15,
    }
  });

  export {styles};